<p class="text-gray-700 text-base mb-4">Hello <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p class="text-gray-700 text-base mb-4">
    We're contacting you to notify you that your email is being changed to <span class="font-semibold text-indigo-600"><%= @resource.unconfirmed_email %></span>.
  </p>
<% else %>
  <p class="text-gray-700 text-base mb-4">
    We're contacting you to notify you that your email has been changed to <span class="font-semibold text-indigo-600"><%= @resource.email %></span>.
  </p>
<% end %>